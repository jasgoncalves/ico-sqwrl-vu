export class Clazz {

    id: string;
    label: string;
    uri: string;
    description: string;   
    
    constructor(id: string, label: string, uri: string, description: string)
    {
        this.id = id;
        this.label = label;
        this.uri = uri;
        this.description = description;
    }

}